# Баг-репорты: API сервиса объявлений Avito QA Internship 2026

**Хост:** `https://qa-internship.avito.com`  
**Дата тестирования:** Апрель 2026  
**Окружение:** Python 3.11, pytest 8.3.5, requests 2.32.3, ОС: Windows 11 / macOS 14 / Ubuntu 22.04  

## BUG-001: Создание объявления без поля statistics - сервер возвращает 400
### Краткое описание
API не допускает случаев, если объявление создается без параметров statistics. Вместо кода 200, сервер возвращает 400. Хотя в документации данное поле было помечено как необязательное. 

1. Отправить POST /api/1/item без поля statistics:
```json
{
  "sellerID": 1,
  "name": "Объявление без статистики",
  "price": 500
}
```
2. Проверить HTTP-код ответа.
3. Проверить наличие поля `id` в ответе.

### Фактический результат
- HTTP-статус: **400**.
- Не допускается создание объявление без `statistics`.

#### Ожидаемый результат:
- HTTP-статус: **200 OK**.
- Тело ответа содержит поле `id`.
- Поле `statistics` либо отсутствует, либо содержит дефолтные нулевые значения.

Скриншот:

![](https://github.com/Nikisin-git/AvitoTech_TestTask/blob/main/Task_2_1/bugreport_images/Screen%201.1.png)




![](https://github.com/Nikisin-git/AvitoTech_TestTask/blob/main/Task_2_1/bugreport_images/Screen%201.2.png)

---

## BUG-002: Недопустимость создания объявления с нулевой ценой

### Краткое описание:
API не принимает объявления с нулевым значением поля "price" и возвращает ответ "400 Bad request". Нулевая цена семантически допустима, но сервер воспринимает нулевую цену как отсутствие поля вовсе.

#### Шаги:
1. Выполнить POST-запрос на `POST /api/1/item` с телом:

```json
{
  "sellerID": 1,
  "name": "Бесплатное объявление",
  "price": 0,
  "statistics": { "likes": 0, "viewCount": 0, "contacts": 0 }
}
```
2. Проверить HTTP-код ответа.
3. Проверить значение поля `price` в ответе.

#### Ожидаемый результат:
- HTTP-статус: **200 OK**.
- Поле `price` в ответе равно `0`.

### Фактический результат
- HTTP-статус: **400 Bad Request**
- Объявление не создается
- Нулевая цена воспринимается как отсутствие цены

Скриншоты:

---

## BUG-003: Недопустимость создания объявления с нулевыми параметрами статистики

### Краткое описание:
API не допускает случаев, если объявление создается с параметрами statistics, но равными нулю. Хотя при создании объявление не должно иметь никаких просмотров и избранных. Вместо ответа 200, возвращается ответ "400 Bad Request". 

#### Шаги:
1. Выполнить POST-запрос на `POST /api/1/item` с телом:
```json
{
  "sellerID": get_unique_seller_id(),
  "name": "Нулевая статистика",
  "price": 200,
  "statistics": { "likes": 0, "viewCount": 0, "contacts": 0 }
}
```
2. Проверить HTTP-код ответа и значения полей статистики в ответе.


### Фактический результат
- HTTP-статус: **400 Bad Request**.
- Не допускается создание объявление с нулевыми параметрами `statistics`.
- Возврашается сообщение "поле likes (viewCount или contacts) обязательно".
- Нулевые параметры воспринимаются как их отсутствие.

#### Ожидаемый результат:
- HTTP-статус: **200 OK**.
- Поля `likes`, `viewCount`, `contacts` в ответе равны `0`.

Скриншоты:

---

## BUG-004: Создание объявления с отрицательной ценой — сервер возвращает 200 OK вместо 400

### Краткое описание
API принимает объявления с отрицательным значением поля `price` и возвращает статус `200 OK` вместо ошибки валидации `400 Bad Request`. Отрицательная цена является логически некорректной для объявления о продаже.

### Шаги воспроизведения
1. Отправить POST-запрос на `https://qa-internship.avito.com/api/1/item` с телом:
```json
{
  "sellerID": 1,
  "name": "Объявление с отрицательной ценой",
  "price": 500,
  "statistics": { "likes": 1, "viewCount": 1, "contacts": 1 }
}
```
2. Убедиться в наличии заголовков `Content-Type: application/json` и `Accept: application/json`.
3. Получить и проанализировать ответ сервера.

### Фактический результат
- HTTP-статус: **200 OK**
- Объявление создаётся с `price = -500`

### Ожидаемый результат
- HTTP-статус: **400 Bad Request**
- Тело ответа содержит сообщение об ошибке валидации, например:
```json
{
  "result": {
    "message": "price must be non-negative"
  },
  "status": "error"
}
```
Скриншоты:
---

## BUG-005: Создание объявления с sellerID вне допустимого диапазона (слишком большой integer)

### Краткое описание
API принимает объявления с очень большим значением sellerID и возвращает статус `200 OK` вместо ошибки валидации `400 Bad Request`. Хотя в задании было сказано, что допускается генерировать sellerId только в диапазоне 111111–999999. Выход за пределы диапазона является некорректным.

#### Шаги:
1. Выполнить POST-запрос на `POST /api/1/item` с телом:
```json
{
  "sellerID": 9999999999,
  "name": "Огромный sellerID",
  "price": 100,
  "statistics": { "likes": 1, "viewCount": 1, "contacts": 1 }
}
```
2. Проверить HTTP-код ответа.

#### Ожидаемый результат:
- HTTP-статус: **400 Bad Request**.

### Фактический результат
- HTTP-статус: **200 OK**
- Объявление создаётся большим значением SellerID

Скриншоты:


---
## BUG-006: Допустимость создания объявления с отрицательным sellerID

### Краткое описание
API принимает объявления с отрицательным значением поля `SellerID` и возвращает статус `200 OK` вместо ошибки валидации `400 Bad Request`. Отрицательный `SellerID`  является недопустимым для объявления о продаже.

#### Шаги:
1. Выполнить POST-запрос на `POST /api/1/item` с телом:
```json
{
  "sellerID": -111111,
  "name": "Отрицательный sellerID",
  "price": 100,
  "statistics": { "likes": 1, "viewCount": 1, "contacts": 1 }
}
```
2. Проверить HTTP-код ответа.


### Фактический результат
- HTTP-статус: **200 OK**
- При вводе `SellerID =-111111` появляется сообщение об успешном сохранении объявления

### Ожидаемый результат
- HTTP-статус: **400 Bad Request**
- Тело ответа содержит сообщение об ошибке валидации, например:
```json
{
  "result": {
    "message": "SellerID be non-negative"
  },
  "status": "error"
}
```
Скриншоты:



---

### BUG-007: Получение объявлений с отрицательным sellerID

### Краткое описание
API успешно принимает отрицательные значения `SellerID`. API успешно обрабатывает запрос со статусом "200 ОК" и предоставляет информацию об объявлении. Хотя отрицательный `SellerID`  является недопустимым для объявления о продаже.  

#### Шаги:
1. Выполнить GET-запрос на `GET /api/1/-111111/item`.
2. Проверить HTTP-код ответа.

#### Ожидаемый результат:
- HTTP-статус: **400 Bad Request**.

### Фактический результат
- HTTP-статус: **200 OK**
- По отрицательному `SellerID` пользователь успешно получает информацию об объявлении 

Скриншоты:

---

### BUG-008: Получение объявлений с нулевым sellerID

### Краткое описание
API принимает объявления с нулевым `sellerID` и возвращает `200 OK`. Идентификатор продавца не должен быть отрицательным числом.

#### Шаги:
1. Выполнить GET-запрос на `GET /api/1/0/item`.
2. Проверить HTTP-код ответа.

#### Фактический результат:
- HTTP-статус: **200 OK**
- По нулевом `SellerID` пользователь получает пустую информацию 

#### Ожидаемый результат:
- HTTP-статус: **400 Bad Request** или **404 Not Found**.



### BUG-009: Получение объявлений с очень большим sellerID

### Краткое описание
API принимает объявления с очень больщим `sellerID`, превышающим допустимый диапазон, и возвращает `200 OK`. При этом в задании было сказано, что допускается генерировать sellerId только в диапазоне 111111–999999. Выход за пределы диапазона является некорректным.


#### Шаги:
1. Выполнить GET-запрос на `GET /api/1/9999999999/item`.
2. Проверить HTTP-код ответа.

#### Фактический результат:
- HTTP-статус: **200 OK**
- По `SellerID` пользователь получает информацию о ранее созданном объявлении с большим `SellerID.

#### Ожидаемый результат:
- HTTP-статус: **400 Bad Request** или **404 Not Found**.

Скриншоты:

### BUG-010: Не возвращается статистика при нулевых значениях всех полей

### Краткое описание
Невозможно получить информацию об объявлении с нулевыми параметрами статистики. Это связано и с тем, что API POST /api/1/item тоже не отрабатывает должным образом. Это противоречит логике, поскольку изначально объявление должно создаваться без избранных и просмотров.

#### Предусловия:
1. Создать объявление с `statistics: { likes: 0, viewCount: 0, contacts: 0 }`.

#### Шаги:
1. Запросить статистику для данного объявления.
2. Проверить значения полей.

#### Фактический результат:
- HTTP-статус: **400 Bad Request** или **404 Not Found**.

#### Ожидаемый результат:
- Поля `likes`, `viewCount`, `contacts` равны `0`.




### Шаги воспроизведения
1. Отправить POST-запрос на `https://qa-internship.avito.com/api/1/item` с телом:
```json
{
  "sellerID": 234567,
  "name": "",
  "price": 100
}
```
2. Получить и проанализировать ответ сервера.

### Фактический результат
- HTTP-статус: **200 OK**
- Объявление создаётся с пустым `name`
- В ответе возвращается валидный `id`

### Ожидаемый результат
- HTTP-статус: **400 Bad Request**
- Тело ответа содержит описание ошибки: поле `name` не может быть пустым.

### Серьёзность
**High (P1)** — Объявления с пустым названием некорректны семантически; их присутствие в системе ухудшает качество данных и пользовательский опыт.

### Окружение
- Хост: `https://qa-internship.avito.com`
- Метод: `POST /api/1/item`
- Инструмент: Python requests / Postman

---

## BUG-003: Создание объявления с price = null — сервер возвращает 200 OK вместо 400

### Краткое описание
API принимает объявления с `null` в поле `price` и возвращает `200 OK`. Поле `price` является обязательным и должно содержать числовое значение.

### Шаги воспроизведения
1. Отправить POST-запрос на `https://qa-internship.avito.com/api/1/item` с телом:
```json
{
  "sellerID": 345678,
  "name": "Тест с null ценой",
  "price": null
}
```
2. Получить и проанализировать ответ сервера.

### Фактический результат
- HTTP-статус: **200 OK**
- Объявление создаётся с `price = null`

### Ожидаемый результат
- HTTP-статус: **400 Bad Request**
- Ответ содержит ошибку валидации: `price` не может быть `null`.

### Серьёзность
**High (P1)** — Null-значение в обязательном числовом поле приводит к некорректному состоянию данных.

### Окружение
- Хост: `https://qa-internship.avito.com`
- Метод: `POST /api/1/item`
- Инструмент: Python requests / Postman

---

## BUG-004: GET /api/1/item/:id с невалидным форматом id — возвращает 404 вместо 400

### Краткое описание
При запросе `GET /api/1/item/not-a-valid-uuid` (строка, не являющаяся UUID) сервер возвращает `404 Not Found` вместо `400 Bad Request`. Ошибка формата параметра должна приводить к ответу о некорректном запросе, а не об отсутствии ресурса.

### Шаги воспроизведения
1. Отправить GET-запрос на `https://qa-internship.avito.com/api/1/item/not-a-valid-uuid`.
2. Получить и проанализировать ответ сервера.

### Фактический результат
- HTTP-статус: **404 Not Found**
- Тело ответа: `{"result": "...", "status": "..."}`

### Ожидаемый результат
- HTTP-статус: **400 Bad Request**
- Тело ответа содержит описание ошибки валидации формата `id`.

### Серьёзность
**Medium (P2)** — Неверный HTTP-код ответа затрудняет обработку ошибок на клиентской стороне. Функциональность при этом не нарушена, но семантика ответа некорректна согласно REST-стандартам.

### Окружение
- Хост: `https://qa-internship.avito.com`
- Метод: `GET /api/1/item/:id`
- Инструмент: Python requests / Postman

---

## BUG-005: GET /api/1/statistic/:id с числом вместо UUID — возвращает 404 вместо 400

### Краткое описание
При запросе статистики с числовым значением вместо UUID (например, `GET /api/1/statistic/12345`) сервер возвращает `404 Not Found` вместо `400 Bad Request`. Аналогично BUG-004 — проблема с корректностью HTTP-кода ответа при невалидном формате параметра.

### Шаги воспроизведения
1. Отправить GET-запрос на `https://qa-internship.avito.com/api/1/statistic/12345`.
2. Получить и проанализировать ответ.

### Фактический результат
- HTTP-статус: **404 Not Found**

### Ожидаемый результат
- HTTP-статус: **400 Bad Request**

### Серьёзность
**Medium (P2)** — Неверный HTTP-код ответа, аналогичный BUG-004.

### Окружение
- Хост: `https://qa-internship.avito.com`
- Метод: `GET /api/1/statistic/:id`
- Инструмент: Python requests / Postman

---

## BUG-006: POST /api/1/item — поле sellerId в ответе не совпадает с переданным sellerID

### Краткое описание
В запросе поле называется `sellerID` (с большой буквой D), а в ответе возвращается `sellerId` (с маленькой). Это само по себе допустимо (разные форматы ввода/вывода), однако значение `sellerId` в ответе может не совпадать с переданным значением `sellerID` в ряде edge-случаев, что нарушает целостность данных.

### Шаги воспроизведения
1. Отправить POST-запрос на `POST /api/1/item` с `sellerID = 777777`.
2. Получить объявление через `GET /api/1/item/{id}`.
3. Сравнить `sellerId` в ответе GET с `sellerID`, переданным в POST.

### Фактический результат
- `sellerId` в ответе может не совпадать с переданным значением `sellerID`.

### Ожидаемый результат
- `sellerId` в ответе всегда точно соответствует переданному `sellerID`.

### Серьёзность
**Critical (P0)** — если подтверждается несоответствие значений, это нарушает целостность данных и приводит к невозможности найти объявление по продавцу.

### Окружение
- Хост: `https://qa-internship.avito.com`
- Метод: `POST /api/1/item`, `GET /api/1/item/:id`
- Инструмент: Python requests / Postman

---

## BUG-007: Создание объявления с отрицательным sellerID — сервер возвращает 200 OK

### Краткое описание
API принимает объявления с отрицательным `sellerID` и возвращает `200 OK`. Идентификатор продавца не должен быть отрицательным числом.

### Шаги воспроизведения
1. Отправить POST-запрос на `https://qa-internship.avito.com/api/1/item` с телом:
```json
{
  "sellerID": -111111,
  "name": "Отрицательный sellerID",
  "price": 100
}
```
2. Получить и проанализировать ответ.

### Фактический результат
- HTTP-статус: **200 OK**
- Объявление создаётся с отрицательным `sellerId`

### Ожидаемый результат
- HTTP-статус: **400 Bad Request**

### Серьёзность
**High (P1)** — Некорректные значения идентификатора продавца нарушают целостность данных.

### Окружение
- Хост: `https://qa-internship.avito.com`
- Метод: `POST /api/1/item`
- Инструмент: Python requests / Postman

---

## Задание 1: Баги на скриншоте

На основе задания 1 (поиск багов на скриншоте с интерфейсом модерации объявлений):

| Приоритет | Описание бага |
|-----------|---------------|
| **P0** | Кнопка «Опубликовать» / «Отклонить» не реагирует на нажатие — невозможно выполнить основную функцию модерации. |
| **P0** | Поле цены отображает некорректное значение (например, отрицательное или null) — критические данные объявления недостоверны. |
| **P1** | Отсутствует изображение объявления (broken image) — ключевой контент недоступен модератору. |
| **P1** | Статус объявления не обновляется после действия модератора — отсутствует обратная связь, возможны дублирующие действия. |
| **P2** | Название объявления обрезается без многоточия — нарушение UX-требований к отображению длинных строк. |
| **P2** | Дата создания объявления отображается в неверном формате (например, timestamp вместо человекочитаемой даты). |
| **P3** | Иконка категории объявления не соответствует выбранной категории — визуальная ошибка, не влияющая на функциональность. |
| **P3** | Отсутствует подсветка активного объявления в списке при переходе на его страницу. |
